<guidelines>
  <workflow>
    <overview>
      Tidy documentation after coding and testing are complete. Review code
      comments, usage guides, the README, and the documentation directory to
      ensure they are accurate and simple.
    </overview>

    <step name="start">
      <description>Always ask whether to document the current commit or the current branch before proceeding.</description>
      <question>"Do you want to document the current commit or the current branch?"</question>
    </step>

    <step name="code_comments">
      <remove>Comments that repeat the function or variable name.</remove>
      <remove>Retro-compatibility notes, unless the old behaviour is still exercised at runtime.</remove>
      <add>Only where the code is genuinely confusing or carries a non-obvious technical constraint.</add>
      <keep>A verbose comment that still aids a new reader. State why it was retained.</keep>
    </step>

    <step name="usage_docs">
      <verify>Every command, flag, and workflow described matches the current implementation.</verify>
      <update>The README and the usage guides, so they reflect the change under review.</update>
      <check>Cross-references between documentation files still resolve.</check>
    </step>

    <step name="page_prose">
      <verify>A page describes the system as it is now, not the work that produced it. Strip slice letters ("slice C"), milestone identifiers ("M2b", "M3"), behaviour numbers ("behaviour 12"), pull request numbers, commit hashes, and "shipped / will ship / arrives in" sequencing from page prose — user-facing and internal alike. That vocabulary belongs in the plan, the commit message, and the pull request: each is permanent and searchable.</verify>
      <allow>A short, plain limitations note: a capability limit, not a schedule.</allow>
      <allow>A link to the plan where the reasoning behind a decision is genuinely wanted.</allow>
      <forbid>A project timeline in a page: which branch family did the work, which slice ships next, what the next milestone adds.</forbid>
      <self_check>Before finishing, run:
        grep -rniE '\bslice [a-z]\b|\bm[0-9]+[a-z]?\b|behaviour [0-9]+|milestone|\bshipped\b|\bplanned\b|pull request|commit [0-9a-f]{7,40}' docs/ README.md
        Skip machine-generated files (e.g. docs/configuration.md); every other page must return no hits. Each hit is either delivery vocabulary to rewrite or a spot that needs a link to the plan instead. Fix or justify every hit before finishing.</self_check>
    </step>

    <step name="structure">
      <audit>Orphaned or redundant documentation files.</audit>
      <confirm>File naming follows this repository's convention.</confirm>
      <ensure>The README points at the correct entry-point guides.</ensure>
    </step>
  </workflow>

  <best_practices>
    <rule>Comment the "why", never the "what". This applies to docstrings and module docs, not just inline comments.</rule>
    <rule>Historical context belongs in the VCS log, the plan, the commit message, and the pull request — not in source files and not in documentation pages. The rule covers Markdown prose the same way it covers code.</rule>
    <rule>A page describes the system as it is now, not the work that produced it. Slice letters, milestone identifiers, behaviour numbers, pull request numbers, commit hashes, and "shipped / planned" sequencing are delivery vocabulary: they belong in the plan, the commit message, and the pull request, each of which is permanent and searchable.</rule>
    <rule>A capability limit is useful; a schedule is not. "The router does not restart crashed containers yet" tells a reader what will not work and stays. "This is slice C of M2b, and slice D adds the manager" is a delivery schedule and is cut; the reader who wants the plan gets a link to it. One limits a reader's expectations, the other narrates your delivery.</rule>
    <rule>User-facing pages open in plain language a non-developer can follow, and show a worked example with real input and real output before an abstract description. Define a term on first use or do not use it. The model is docs/configuration-guide.md: it opens "Let's build a tool-swap config together, starting from the smallest one that works", then shows a three-line YAML file that actually runs.</rule>
    <rule>Internal design pages may be technical — seams, protocols, and developer audiences are fine — but the delivery-vocabulary ban applies to them equally.</rule>
    <rule>Link to the plan, do not duplicate it. Where the reasoning behind a decision is genuinely wanted, a link to the plan section is the page's obligation; restating the reasoning in the page is how the narrative creeps back in.</rule>
    <rule>Usage documentation must mirror the current, working implementation.</rule>
    <rule>Keep the documentation tree flat: one guide per concern.</rule>
    <rule>Never document a flag or command from memory. Confirm it against the code first.</rule>
  </best_practices>

  <constraints>
    <file_scoping>
      Edit documentation files (*.md, *.txt, *.rst, *.template, README, doc/).
      Source files (*.py, *.sh, *.js, *.ts, *.json, *.yml, *.yaml) may be edited
      **only to add or remove comments** — never change logic, signatures, or
      indentation.
    </file_scoping>
  </constraints>

  <communication>
    <tone>Direct and technical. No conversational openers or closings.</tone>
    <initial>Ask commit vs branch before starting.</initial>
    <completion>State the scope, list the files reviewed and changed, summarise what was removed, added, and deliberately kept, and report the page_prose self-check: the grep was run and every hit fixed or justified.</completion>
  </communication>
</guidelines>
