[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tool-swap"
version = "0.1.0"
description = "A tool swapping router for managing AI agent tool contexts."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [
    {name = "iar3-r8", email = "dev@iar3-r8.io"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "typer>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "mypy",
    "import-linter",
    "typer",
]

[project.scripts]
tswap = "tool_swap.__main__:main"

[tool.hatch.build]
packages = ["src/tool_swap"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "A", "UP", "B", "C4", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true

[tool.pytest.ini_options]
asyncio_mode = "strict"
testpaths = ["tests"]
markers = [
    "docker: marks tests as requiring Docker (deselect with '-m \"not docker\"')",
    "gpu: marks tests as requiring GPU (deselect with '-m \"not gpu\"')",
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
addopts = "--strict-markers -m 'not docker and not gpu and not slow'"
filterwarnings = ["error"]
